server:
  port: 8761

spring:
  application:
    name: discovery-server
  config:
    activate:
      on-profile: Elias


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://discovery-server-2.mitocode.com:8762/eureka,http://discovery-server-3.mitocode.com:8763/eureka
  datacenter: EliasRoy
  environment: Develoment

  instance:
    hostname: localhost2
    prefer-ip-address: false
    lease-expiration-duration-in-seconds: 10
    lease-renewal-interval-in-seconds: 10

---
server:
  port: 8762

spring:
  application:
    name: discovery-server-2
  config:
    activate:
      on-profile: Roy


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://discovery-server.mitocode.com:8761/eureka,http://discovery-server-3.mitocode.com:8763/eureka
  datacenter: EliasRoy
  environment: Develoment

  instance:
    hostname: localhost2
    prefer-ip-address: false
    lease-expiration-duration-in-seconds: 10
    lease-renewal-interval-in-seconds: 10
---

server:
  port: 8763

spring:
  application:
    name: discovery-server-3
  config:
    activate:
      on-profile: Juan


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://discovery-server.mitocode.com:8761/eureka,http://discovery-server-2.mitocode.com:8762/eureka
  datacenter: EliasRoy
  environment: Develoment

  instance:
    hostname: localhost2
    prefer-ip-address: false
    lease-expiration-duration-in-seconds: 10
    lease-renewal-interval-in-seconds: 10